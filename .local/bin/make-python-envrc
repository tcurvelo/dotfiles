#!/bin/bash
[ $# -lt 1 ] \
    && echo -ne "Usage:\n\t "$(basename $0)" <python version>\n" 2> /dev/stderr \
    && exit 1

VERSION=$1

cat <<EOF >> .envrc
layout pyenv $VERSION
EOF

direnv allow .
direnv exec . pip install -U pip
